# Test_Dependency_Injection

Small sample project to demonstrate dependency injection patterns and a simple service interface (`IMyService`).

How to run:

```bash
cd Test_Dependency_Injection
dotnet run
```

Notes:

- Contains a small `Program.cs` and sample `IMyService` implementation.
- Useful for learning service registration and constructor injection in .NET.
